import React from 'react';
import styles from './AutoSlider.module.css';

export default function AutoSlider() {
    return (
        <div className={styles.slider}>
            Odds Slider with calculation
        </div>
    );
}
